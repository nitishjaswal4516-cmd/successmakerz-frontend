image: node:20

stages:
  - install
  - build

install_dependencies:
  stage: install
  script:
    - cd frontend
    - npm install

build_project:
  stage: build
  script:
    - cd frontend
    - npm run build