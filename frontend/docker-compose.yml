services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: successmakerz-frontend
    ports:
      - "3000:80"
    restart: unless-stopped
    networks:
      - frontend-network

networks:
  frontend-network:
    driver: bridge
